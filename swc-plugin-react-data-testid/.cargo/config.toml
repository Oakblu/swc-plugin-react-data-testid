# Do NOT set a global build target here — cargo test must compile to the native host.
# To build the WASM artifact for publishing, run:
#   cargo build --target wasm32-wasip1 --release
#
# To install the target if missing:
#   rustup target add wasm32-wasip1
